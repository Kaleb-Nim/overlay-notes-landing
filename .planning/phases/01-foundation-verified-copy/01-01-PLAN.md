---
phase: 01-foundation-verified-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - bun.lock
  - .gitignore
  - next.config.ts
  - tsconfig.json
  - next-env.d.ts
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-04, FOUND-05]
user_setup: []

must_haves:
  truths:
    - "Running `bun run dev` serves the Next.js 16 App Router site at `/` under TypeScript (FOUND-01)"
    - "The page background renders the ruled-paper `repeating-linear-gradient` at the prototype's 31px/32px rhythm (FOUND-05)"
    - "All 12 design tokens (paper, rule, margin-red, purple, purple-dark, ink, head, body, dark, dark-muted, accent-red, yellow) exist as `:root` CSS custom properties consumed via var() (FOUND-04)"
    - "Shantell Sans and Public Sans load self-hosted via `next/font/google` at the weight/style superset the design uses, with no render-blocking third-party font `<link>` (FOUND-03)"
    - "`bun run build` completes with exit 0 on Next 16.2.11 + Turbopack"
    - "The pre-existing Playwright harness (playwright.config.ts, scripts/, tests/, TESTING.md) is untouched and `bash scripts/test-gate.sh` exits 0 (inert) because `@playwright/test` is not installed"
  artifacts:
    - package.json
    - next.config.ts
    - tsconfig.json
    - next-env.d.ts
    - app/layout.tsx
    - app/page.tsx
    - app/globals.css
  key_links:
    - "app/layout.tsx sets `--font-shantell`/`--font-public` variable classes on `<html>` so app/globals.css `font-family: var(--font-...)` resolves"
    - "app/layout.tsx imports app/globals.css so tokens + ruled-paper background apply globally"
---

<objective>
Stand up the walking skeleton for Overlay Notes' landing page: a running Next.js 16 App
Router + TypeScript project, scaffolded by hand (create-next-app refuses in this non-empty
repo), that serves `/` rendering the ported design tokens, the ruled-paper background, and
both self-hosted typefaces — proven by `bun run dev` serving the page and `bun run build`
exiting 0.

Purpose: Every Phase 2 section is built against these tokens, fonts, and background. Nothing
visible can be built until the design-system foundation compiles and renders.
Output: package.json + 5 config/app files + globals.css, all pinned to the RESEARCH-verified
stack, with the pre-built Playwright harness preserved and its gate left inert.
</objective>

## Phase Goal

**As a** margin-scribbling reader arriving from search or a shared link, **I want to** load the Overlay Notes landing page and have it render on its hand-drawn ruled-paper foundation with the right typefaces, **so that** every product section built on top of it inherits the locked, pixel-faithful design system from the very first commit.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/01-foundation-verified-copy/01-CONTEXT.md
@.planning/phases/01-foundation-verified-copy/01-RESEARCH.md
@.planning/phases/01-foundation-verified-copy/01-UI-SPEC.md
@.planning/design-handoff/concept-1a.html
@TESTING.md
@scripts/test-gate.sh
</context>

## Artifacts this phase produces (whole phase — for downstream reference)

Plan 01-01 (this plan): `package.json`, `bun.lock`, `.gitignore`, `next.config.ts`,
`tsconfig.json`, `next-env.d.ts`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`.
Plan 01-02 (parallel): `lib/site-config.ts` (`siteConfig.baseUrl`), `lib/content.ts`
(`Faq` interface, `faqs`, `originStory`), `scripts/verify-claims.ts`,
`.planning/phases/01-foundation-verified-copy/01-CLAIM-TRACEABILITY.md`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Manual Next.js 16 scaffold and pinned dependency install (preserve the test harness, do NOT install Playwright)</name>
  <read_first>
    - .planning/phases/01-foundation-verified-copy/01-RESEARCH.md (Pattern 1 "Manual scaffold", the exact five-file set, exact pinned versions in the Installation block, Common Pitfall #1 Playwright timing, Common Pitfall #4 tsconfig auto-rewrite)
    - .planning/phases/01-foundation-verified-copy/01-CONTEXT.md (Stack + Test-harness decisions: Bun as installer/runner only, NO `--bun`, DEFER Playwright to Phase 2)
    - .claude/CLAUDE.md (Bun compatibility section, package.json scripts rule)
    - scripts/test-gate.sh (the three self-skip conditions — understand why leaving @playwright/test uninstalled keeps it inert)
    - TESTING.md ("active from Phase 2 on")
  </read_first>
  <files>package.json, bun.lock, .gitignore, next.config.ts, tsconfig.json, next-env.d.ts</files>
  <action>
    Do NOT run `create-next-app` — RESEARCH Pattern 1 confirms it refuses against this repo's
    existing TESTING.md/playwright.config.ts/scripts/tests and offers no bypass flag. Scaffold
    by hand.

    Install runtime deps at the RESEARCH-pinned exact versions (not `latest` — `latest` resolves
    to a TS 7.x prerelease and a Node 26 types line):
    `bun add next@16.2.11 react@19.2.8 react-dom@19.2.8` and
    `bun add -d typescript@5.9.3 @types/node@^22 @types/react@19.2.17 @types/react-dom@19.2.3`,
    then `bun add @vercel/analytics@2.0.1 @vercel/speed-insights@2.0.0` (deps installed now;
    Task 2 wires them; live reporting is a Phase 4 concern). All ten packages are pre-cleared by
    RESEARCH's Package Legitimacy Audit (six flagged `[SUS]` on the `too-new` signal only — all
    official high-download repos; keep-and-override, no human checkpoint; none ship a postinstall).

    Do NOT run `bun add -d @playwright/test` and do NOT run `bunx playwright install chromium` —
    that flips all three of scripts/test-gate.sh's skip conditions false at once (config + specs
    already exist in the repo) and makes the regression gate run tests/landing.spec.ts against
    Phase-2 surfaces that do not exist yet, halting the phase (RESEARCH Common Pitfall #1, Option
    A). Playwright is Phase 2's first task.

    Hand-author the config files exactly as RESEARCH Pattern 1 gives them:
    `next.config.ts` exporting an empty `NextConfig` (no `output: 'export'` override);
    `tsconfig.json` with `jsx: "react-jsx"` from the start (avoids Next's first-build auto-rewrite,
    Pitfall #4), `moduleResolution: "bundler"`, `plugins: [{ name: "next" }]`, `paths: { "@/*": ["./*"] }`,
    and `include` covering `next-env.d.ts`, `**/*.ts`, `**/*.tsx`, `.next/types/**/*.ts`;
    `next-env.d.ts` with the two `/// <reference ... />` lines and the do-not-edit note.

    Set package.json `scripts` to `dev: "next dev"`, `build: "next build"`, `start: "next start"`,
    `test: "playwright test"` — NEVER add `--bun` to any script (open bun issues #24829/#25014/#26244
    break `next` under the Bun runtime). The `test` script line is harmless because the GSD gate calls
    scripts/test-gate.sh, not `bun test`, and Playwright stays uninstalled this phase.

    Create `.gitignore` (none exists at repo root): ignore `node_modules/`, `.next/`, `next-env.d.ts`
    is intentionally NOT ignored (commit it). Do not touch, move, or overwrite playwright.config.ts,
    scripts/test-gate.sh, tests/landing.spec.ts, tests/seo.spec.ts, or TESTING.md.
  </action>
  <verify>
    <automated>test -f package.json && test -f next.config.ts && test -f tsconfig.json && test -f next-env.d.ts && grep -Fq '"next": "16.2.11"' package.json && grep -Fq '"react": "19.2.8"' package.json && grep -Fq '"typescript": "5.9.3"' package.json && grep -Fq '"react-jsx"' tsconfig.json && ! test -e node_modules/.bin/playwright && bash scripts/test-gate.sh | grep -q "skipping"</automated>
    <human-check>Confirm playwright.config.ts, scripts/test-gate.sh, tests/*.spec.ts and TESTING.md are byte-identical to before scaffolding (git status shows them unmodified).</human-check>
  </verify>
  <acceptance_criteria>
    - `bun install` / the `bun add` chain completes; `package.json` pins `next@16.2.11`, `react@19.2.8`, `react-dom@19.2.8`, `typescript@5.9.3`, and lists `@vercel/analytics` + `@vercel/speed-insights`.
    - `create-next-app` was never invoked (no CNA marker files, no overwritten harness files).
    - `@playwright/test` is NOT installed: `node_modules/.bin/playwright` does not exist.
    - `bash scripts/test-gate.sh` exits 0 and its stdout contains the word `skipping` (proving the gate is correctly inert for Phase 1).
    - `tsconfig.json` contains `"jsx": "react-jsx"`; `next.config.ts` has no `output: 'export'`.
    - `package.json` scripts contain no `--bun` flag anywhere.
    - `.gitignore` ignores `node_modules/` and `.next/`; the five pre-existing harness files are unmodified.
  </acceptance_criteria>
  <done>Repo has a hand-scaffolded Next.js 16 App Router + TypeScript project with pinned deps, the test harness preserved, and the Playwright gate self-skipping.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Self-host both fonts and port the prototype global CSS (tokens + ruled-paper background) into the root layout</name>
  <read_first>
    - .planning/phases/01-foundation-verified-copy/01-RESEARCH.md (Pattern 2 next/font/google discrete-weight loader with the verified `weight`/`style`/`variable`/`subsets`/`display` call, Pattern 3 CSS-porting rules and the two font-family swaps, Common Pitfall #2 the Shantell 500-normal superset justification)
    - .planning/design-handoff/concept-1a.html (lines 40-137: the `:root` token block, the `body` ruled-paper background, and every ported selector; line 37 the font source of truth)
    - .planning/phases/01-foundation-verified-copy/01-UI-SPEC.md (Font Loading Contract, Color token table, Spacing ruled-paper rhythm)
    - .planning/phases/01-foundation-verified-copy/01-CONTEXT.md (font-weight correction: load the superset; Analytics/SpeedInsights via `/next` subpath)
  </read_first>
  <files>app/layout.tsx, app/globals.css</files>
  <action>
    In `app/layout.tsx`, import `Shantell_Sans` and `Public_Sans` from `next/font/google` and
    import `./globals.css`. Configure BOTH with the RESEARCH-verified superset:
    `weight: ['400','500','600','700']`, `style: ['normal','italic']`, `subsets: ['latin']`
    (mandatory — preload defaults true and omitting subsets throws at build), `display: 'swap'`,
    and `variable: '--font-shantell'` / `variable: '--font-public'`. The superset covers both the
    prototype `<link>` reading and the grep-verified actual usage (Shantell 500 normal in
    `.note-r`/`.note-b`/`.who .kicker`/`.origin p`) — unused `@font-face` faces are never fetched,
    so this costs nothing at runtime. Drop Space Grotesk entirely (prototype loads it, references
    it nowhere). Set `<html lang="en" className={\`${shantellSans.variable} ${publicSans.variable}\`}>`
    and render `{children}` inside `<body>`. Export a minimal `metadata` object with `title: 'Overlay Notes'`
    (full SEO metadata is Phase 3 — do not add description/openGraph/canonical here). Wire analytics:
    import `Analytics` from `@vercel/analytics/next` and `SpeedInsights` from `@vercel/speed-insights/next`
    (the `/next` subpath, NOT `/react`) and render both at the end of `<body>` — deps only, live data is Phase 4.

    Port `concept-1a.html`'s `<style>` block into `app/globals.css` near-verbatim (RESEARCH Pattern 3):
    copy the entire `:root` block with all 12 tokens EXACTLY (paper, rule, margin-red, purple,
    purple-dark, ink, head, body, dark, dark-muted, accent-red, yellow — literal numbers, do not
    re-derive), copy the `body` ruled-paper `background: repeating-linear-gradient(var(--paper) 0 31px,
    var(--rule) 31px 32px), linear-gradient(var(--paper),var(--paper));` verbatim, and carry over the
    remaining prototype selectors (`.wrap`, `.marker`, nav/hero/who/band/diff/origin/footer, the
    `@media(max-width:760px)` block) unchanged. Make EXACTLY two edits while porting: replace
    `'Shantell Sans',cursive` → `var(--font-shantell), cursive` and `'Public Sans',...` →
    `var(--font-public), ...` everywhere they appear (including inside `font:` shorthands). Setting
    these selectors now is harmless — they target markup Phase 2 will add.
  </action>
  <verify>
    <automated>grep -Fq 'repeating-linear-gradient(var(--paper) 0 31px' app/globals.css && grep -Fq '--paper:#f6f1e7' app/globals.css && grep -Fq '--dark-muted:#a9a4c4' app/globals.css && grep -Fq '--yellow:#ffc93c' app/globals.css && grep -Fq 'var(--font-shantell)' app/globals.css && grep -Eq "from 'next/font/google'" app/layout.tsx && grep -q 'Shantell_Sans' app/layout.tsx && grep -q 'Public_Sans' app/layout.tsx && grep -q "'700'" app/layout.tsx && grep -q "'italic'" app/layout.tsx && grep -Fq '@vercel/analytics/next' app/layout.tsx && grep -Fq 'lang="en"' app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains `repeating-linear-gradient(var(--paper) 0 31px, var(--rule) 31px 32px)` verbatim and all 12 `:root` tokens with their exact prototype values.
    - `app/globals.css` references `var(--font-shantell)` and `var(--font-public)` (the only two porting edits) and contains no remaining `'Shantell Sans'` / `'Public Sans'` string literals.
    - `app/layout.tsx` imports `Shantell_Sans` and `Public_Sans` from `next/font/google`, each with `weight: ['400','500','600','700']`, `style: ['normal','italic']`, `subsets: ['latin']`, `display: 'swap'`, and a `variable`; Space Grotesk is not loaded.
    - `app/layout.tsx` sets `<html lang="en">` with both font variable classes and imports `Analytics`/`SpeedInsights` from the `/next` subpath.
    - No third-party font `<link>` to `fonts.googleapis.com` exists in `app/` (self-hosting is provided by `next/font/google`).
  </acceptance_criteria>
  <done>Both typefaces load self-hosted at the design's weights/styles, and the ported tokens + ruled-paper background are wired globally through the root layout.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Proof-of-foundation page and full walking-skeleton verification (dev serves, build passes)</name>
  <read_first>
    - .planning/phases/01-foundation-verified-copy/01-RESEARCH.md (Recommended Project Structure — `app/page.tsx` "Phase 1: minimal proof-of-tokens placeholder"; the verified `bun run build` → success with Turbopack)
    - .planning/design-handoff/concept-1a.html (`.wrap` container, token names, Shantell heading pattern — for a faithful-feeling placeholder only, NOT any real Phase 2 section)
    - .planning/phases/01-foundation-verified-copy/01-CONTEXT.md (Phase Boundary — build NO visible page sections; this is a placeholder that proves the foundation renders)
  </read_first>
  <files>app/page.tsx</files>
  <action>
    Author `app/page.tsx` as a Server Component default export returning a MINIMAL proof-of-foundation
    placeholder — explicitly NOT any of the 10 real sections (nav, hero, who, band, diff, origin, FAQ,
    support, footer are all Phase 2, per the Phase Boundary). Render a single `.wrap` container with one
    Shantell-Sans heading (e.g. a short "Overlay Notes" wordmark using the `.marker` family or an inline
    `var(--font-shantell)` style) and one short line of Public Sans body text, using at least one design
    token color (e.g. `color: var(--purple)`) so a human eye can confirm both typefaces + a token + the
    ruled-paper background all render together. Keep it a few lines; do not import `lib/content.ts` or any
    Phase 2 copy. Then verify the whole slice runs end to end: `bun run build` must exit 0 (Next 16.2.11 +
    Turbopack, verified reproducible in RESEARCH), and `bun run dev` must serve the page at `/`.
  </action>
  <verify>
    <automated>test -f app/page.tsx && grep -Eq 'export default' app/page.tsx && bun run build</automated>
    <human-check>Run `bun run dev`, open http://localhost:3000/ — confirm the ruled-paper horizontal-line background is visible, the heading renders in the handwritten Shantell Sans face, the body line renders in Public Sans, and a purple token color is applied. No console errors.</human-check>
  </verify>
  <acceptance_criteria>
    - `bun run build` exits 0 with no type errors (output shows `Next.js 16.2.11 (Turbopack)`).
    - `bun run dev` serves `/` returning HTTP 200 with the rendered placeholder.
    - `app/page.tsx` is a default-export component that renders inside `.wrap`, uses `var(--font-shantell)` (directly or via `.marker`) and at least one design token, and imports no Phase 2 sections or `lib/content.ts`.
    - The built HTML self-hosts fonts (generated CSS references hashed woff2 under `.next`, no request to `fonts.googleapis.com`).
  </acceptance_criteria>
  <done>`bun run dev` serves a page proving tokens + both fonts + ruled-paper background render together, and `bun run build` passes — the walking skeleton stands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm/Bun registry → local node_modules | Third-party package code is installed and executed at build time |
| fonts.gstatic.com → build | `next/font/google` fetches woff2 once at build time, then self-hosts (no per-request third-party call) |

This phase ships a fully static site with no forms, no auth, no sessions, no user input, and no
server-side data handling (RESEARCH Security Domain) — STRIDE categories S/R/I/D/E have no
applicable surface in Phase 1. The only live category is Tampering via supply chain (V14).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | Bun/npm installs (next, react, react-dom, typescript, @types/*, @vercel/*) | high | mitigate | Pin exact versions per RESEARCH Installation block; every package pre-cleared by RESEARCH Package Legitimacy Audit (official high-download repos, no postinstall). Six flagged `[SUS]` on the `too-new` signal only — keep-and-override, no human checkpoint needed. |
| T-01-CFG | Tampering | tsconfig / next.config authored by hand | low | accept | Config values taken verbatim from RESEARCH-verified working set; `output: 'export'` deliberately omitted; no runtime input surface to exploit. |
| T-01-IN | Spoofing/Info-disclosure | user input surface | low | accept | No input surface exists this phase — all content is build-time-authored; revisit only if a future milestone adds dynamic behavior. |
</threat_model>

<verification>
- `bun run build` exits 0; `bun run dev` serves `/` at HTTP 200.
- `app/globals.css` carries all 12 tokens + the verbatim ruled-paper gradient; both fonts self-hosted via `next/font/google`; no `fonts.googleapis.com` link.
- `bash scripts/test-gate.sh` exits 0 printing `skipping`; harness files unmodified; `@playwright/test` not installed.
- Package versions pinned exactly; no `--bun` in scripts.
</verification>

<success_criteria>
The Next.js/Bun foundation runs locally (`bun run dev`) under TypeScript, rendering the ported
design tokens and the 31px/32px ruled-paper `repeating-linear-gradient`, with Shantell Sans +
Public Sans self-hosted at the design's weights/styles and no render-blocking third-party font
request — satisfying FOUND-01, FOUND-03, FOUND-04, FOUND-05 — while the pre-built Playwright
harness stays inert and untouched.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-verified-copy/01-01-SUMMARY.md` when done.
</output>
