---
phase: 04-deployment-domain-verification
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [DEPL-03]
user_setup:
  - service: google-cloud-dns
    why: "The `kalebnim.dev` zone is hosted on Google Cloud DNS (nameservers ns-cloud-b{1,2,3,4}.googledomains.com), not Vercel's. The local gcloud CLI is authenticated but pinned to impersonate a deleted service account from an unrelated project (til-ai-2026), so every `gcloud dns` call fails with NOT_FOUND. There is no working automated path — the record must be added by a human in the console."
    dashboard_config:
      - task: "Add a CNAME record: name `overlay-notes`, value = the exact target string printed by `vercel domains inspect overlay-notes.kalebnim.dev` in Task 1. Add nothing else; change nothing else."
        location: "Google Cloud Console → Network Services → Cloud DNS → the managed zone for kalebnim.dev"
      - task: "IF AND ONLY IF Task 1's CLI output requests it, also add the TXT verification record exactly as printed (name and value both copied verbatim)."
        location: "Same zone, same console visit"

must_haves:
  truths:
    - "`https://overlay-notes.kalebnim.dev/` returns HTTP 200 over TLS with a valid certificate whose subject or SAN covers that hostname (DEPL-03)"
    - "`http://overlay-notes.kalebnim.dev/` redirects to the https:// origin rather than serving plaintext (DEPL-03, ASVS V9)"
    - "`dig +short overlay-notes.kalebnim.dev CNAME` returns exactly the project-specific target string read live from `vercel domains inspect` — not a guessed or generic value (RESEARCH Pitfall 1)"
    - "`vercel domains inspect overlay-notes.kalebnim.dev` reports a valid configuration for the `overlay-notes-landing` project"
    - "`curl -sSD - -o /dev/null https://overlay-notes.kalebnim.dev/` shows NO robots response header at all — closing Phase 3's deferred SEO-08 production-branch observation on the real domain (Success Criterion #3)"
    - "`bash scripts/verify-deployment.sh https://overlay-notes.kalebnim.dev` exits 0 — /robots.txt, /sitemap.xml, /og-image.png, the SoftwareApplication JSON-LD and the absolute canonical are all correct on the custom domain"
    - "`dig +short kalebnim.dev A` and `dig +short kalebnim.dev NS` return exactly what they returned before the human touched the zone — the apex site served from the sibling Vercel project is unaffected"
  artifacts: []
  key_links:
    - from: "Google Cloud DNS zone kalebnim.dev"
      to: "Vercel project overlay-notes-landing"
      via: "CNAME `overlay-notes` → the project-specific target read from `vercel domains inspect` (never `cname.vercel-dns.com`)"
      pattern: "vercel-dns"
    - from: "https://overlay-notes.kalebnim.dev"
      to: "lib/site-config.ts baseUrl"
      via: "the live domain now equals the canonical host every absolute URL was built against in Phases 1-3"
      pattern: "overlay-notes\\.kalebnim\\.dev"
  prohibitions:
    - requirement_id: DEPL-03
      category: safety
      status: resolved
      verification: judgment
      statement: "MUST NOT modify, replace, or delete any pre-existing record in the kalebnim.dev Cloud DNS zone. The apex already serves a different live site from a different Vercel project (nim-kaleb, A record 76.76.21.21); only an additive `overlay-notes` record — plus the TXT record if and only if Vercel explicitly requests one — is permitted."
---

## Phase Goal

ROADMAP.md states this phase's goal in outcome form, not user-story form; see 04-01-PLAN.md's
Phase Goal section for the verbatim quote and the planner's note. No user story is invented.

**User-observable increment delivered by this plan:** a visitor can reach the site at the real
address the whole project was built around — `https://overlay-notes.kalebnim.dev` — over HTTPS,
and every canonical, OG, sitemap and JSON-LD URL shipped in Phase 3 now points at a host that
actually resolves.

<objective>
Attach `overlay-notes.kalebnim.dev` to the Vercel project created in 04-01, get the CNAME added
in the Google Cloud DNS console by a human, and verify the live domain serves over HTTPS with no
leftover robots header — closing DEPL-03 and Phase 3's deferred SEO-08 production observation
(ROADMAP Success Criterion #3).

Purpose: this is the last irreversible step, and it runs in wave 2 by design — the ordering
constraint from the ROADMAP is honoured as a real dependency on 04-01, not as a comment.
Output: a live custom domain with a Vercel-managed TLS certificate, and the first production
observation that the robots header is genuinely absent on the real domain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deployment-domain-verification/04-RESEARCH.md
@.planning/phases/04-deployment-domain-verification/04-01-SUMMARY.md
@.planning/phases/03-seo-metadata-structured-data-social-card/03-VERIFICATION.md
@next.config.ts
@lib/site-config.ts
</context>

<artifacts>
## Artifacts This Plan Produces

| Artifact | Kind | Notes |
|----------|------|-------|
| (no repo files) | — | This plan writes nothing into the repository. Its output is external state plus the evidence recorded in its SUMMARY. |
| Domain `overlay-notes.kalebnim.dev` attached to the Vercel project | external state | Created by `vercel domains add`. Practically irreversible once public links exist. |
| CNAME record `overlay-notes` in the `kalebnim.dev` Google Cloud DNS zone | external state, human-created | Value read live from `vercel domains inspect` — never guessed. |
| TXT verification record (conditional) | external state, human-created | Only if the CLI explicitly requests one — see the assumption note on RESEARCH A1 / Open Question 2. |
| Vercel-managed TLS certificate for the subdomain | external state | Auto-issued via HTTP-01 once DNS resolves; no manual cert management, no `vercel certs`. |
| The recorded CNAME target string | evidence in SUMMARY | Needed by any future maintainer re-pointing or decommissioning the domain. |
</artifacts>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback — unresolved rows)

Phase-wide tally: 4 probe rows surfaced, 4 authored as flagged assumptions across
04-01 / 04-02 / 04-03, 0 silently dropped. This plan owns row 3.

| Row | Probe | Planner assumption (flagged, not auto-resolved) |
|-----|-------|--------------------------------------------------|
| DEPL-03 | `unclassified` — review manually | Assumed: "serves on `https://overlay-notes.kalebnim.dev`" means the apex-less subdomain only, with TLS terminated by Vercel's automatic certificate, and does NOT require a `www.` variant, an HSTS policy, a redirect from the `*.vercel.app` alias, or a redirect from the old GitHub Pages URL. None of those are in REQUIREMENTS.md or the ROADMAP success criteria, so none are planned. If the author expects any of them, they are separable follow-ups, not silent omissions. |

### Other flagged assumptions carried from 04-RESEARCH.md

- **A1 / Open Question 2 (TXT verification may or may not be requested).** `kalebnim.dev`'s apex is
  already owned by a sibling project in the same Vercel team, so `vercel domains add` may go
  straight to CNAME or may request an additional TXT record. The plan branches on the actual CLI
  output rather than assuming either way, and both records are handed to the human in the *same*
  checkpoint so only one console visit is needed.
- **A2 / Open Question 1 (the ROADMAP mechanism is partly wrong).** Attaching the domain does not
  flip a noindex switch — Vercel's automatic robots header applies to preview deployments only. The
  wave-2 ordering is kept because verify-before-attach is defensible QA on its own; the actual
  duplicate-content safeguard is the absolute canonical tag shipped in Phase 3. **Do not add header
  or middleware code to de-index the `*.vercel.app` alias** — that is unrequested scope.
- **Timing.** TLS issuance is automatic once DNS resolves, typically within minutes, but DNS
  propagation is outside anyone's control here. A slow first resolve is not a failure; a wrong CNAME
  value is. Distinguish the two by re-reading `vercel domains inspect` rather than retrying blindly.
</flagged_assumptions>

<assumption_delta_decision>
The assumption-delta scanner flagged one `chosen`-class signal in the phase scope: the term
**"custom"** in the ROADMAP goal's phrase *"with the custom domain attached only after content and
SEO are confirmed correct."*

- **Now-primary noun:** "custom domain" — i.e. `overlay-notes.kalebnim.dev`.
- **Decision:** `no-change`.
- **Rationale:** "custom domain" is industry terminology distinguishing a purchased hostname from
  the platform-generated `*.vercel.app` alias; it is not a configurability signal. Exactly one
  domain is in scope, it is hardcoded as `siteConfig.baseUrl`, and REQUIREMENTS.md names it
  literally in DEPL-03 — nothing here becomes parameterized or multi-valued.
</assumption_delta_decision>

<!-- planner-discipline-allow: x-robots-tag -->
<!-- planner-discipline-allow: X-Robots-Tag -->

<tasks>

<task type="auto">
  <name>Task 1: Capture the DNS baseline, attach the domain, and read the project-specific CNAME target live</name>
  <files>(no repo files — creates external state: a domain attached to the Vercel project)</files>
  <read_first>
    - @.planning/phases/04-deployment-domain-verification/04-RESEARCH.md — `## Recommended Command Sequence` step 6, `## Common Pitfalls` 1, `## Don't Hand-Roll` (the CNAME-discovery and TLS rows), `## Assumptions Log` A1, `## Open Questions` 2
    - @.planning/phases/04-deployment-domain-verification/04-01-SUMMARY.md (the exact Vercel project name and team slug produced by wave 1 — use those, do not re-derive)
    - @lib/site-config.ts (the `baseUrl` constant — the domain being attached must match it exactly)
    - The `prohibitions` block in this plan's frontmatter (the zone is shared with a live sibling site)
  </read_first>
  <action>
    (a) BASELINE FIRST — before touching anything, capture the current state of the shared zone so
    the human checkpoint can be proven additive afterwards. Record the verbatim output of
    `dig +short kalebnim.dev NS`, `dig +short kalebnim.dev A`, `dig +short www.kalebnim.dev`, and
    `dig +short overlay-notes.kalebnim.dev CNAME` (expected empty — nothing to undo). Write all four
    into the SUMMARY. 04-RESEARCH.md expects nameservers `ns-cloud-b1..b4.googledomains.com` and apex
    A `76.76.21.21`; if the live values differ, stop and report rather than proceeding.

    (b) Confirm you are linked to the right project: `vercel project inspect overlay-notes-landing`
    should still report the GitHub connection from 04-01. Do not create a second project.

    (c) ATTACH (DEPL-03). Run
    `vercel domains add overlay-notes.kalebnim.dev overlay-notes-landing`, then
    `vercel domains inspect overlay-notes.kalebnim.dev`. Capture the CLI output verbatim.

    (d) READ THE TARGET — do not guess it. From the inspect output, extract the project-specific
    CNAME target for the `overlay-notes` subdomain. Its shape is a per-project hash plus a numbered
    `vercel-dns` suffix (Vercel's own documentation example is `d1d4fc829fe7bc7c.vercel-dns-017.com`
    — that is their example, NOT this project's value). If what you extracted equals
    `cname.vercel-dns.com`, treat that as a mis-read and re-run the inspect: 04-RESEARCH.md Pitfall 1
    and `.claude/CLAUDE.md` both name the generic value as a known failure mode that produces a
    domain which never verifies. Record the exact string, including or excluding the trailing dot
    exactly as printed.

    (e) BRANCH on TXT. If the CLI output requests an additional TXT verification record (RESEARCH
    A1 / Open Question 2 — same-team subdomain delegation may or may not require it), capture its
    name and value verbatim too, and include it in the same checkpoint so the human makes one
    console visit rather than two. If no TXT is requested, say so explicitly in the SUMMARY rather
    than leaving it ambiguous.

    Do not run `vercel certs` — Vercel manages certificates automatically and its own CLI help marks
    that command as advanced-use-only. Do not attempt any `gcloud dns` command: 04-RESEARCH.md
    verified the local gcloud auth is pinned to a deleted service account in an unrelated project,
    and repairing it is explicitly out of scope and risks the author's other workflows.
  </action>
  <verify>
    <automated>vercel domains inspect overlay-notes.kalebnim.dev 2>&1 | tee /tmp/04-domain-inspect.txt; grep -qiE 'vercel-dns' /tmp/04-domain-inspect.txt || { echo "FAIL: no CNAME target found in inspect output"; exit 1; }; grep -qx 'cname.vercel-dns.com' /tmp/04-domain-inspect.txt && { echo "FAIL: generic CNAME value read - re-read the project-specific target"; exit 1; }; dig +short kalebnim.dev NS | sort | tee /tmp/04-zone-ns-baseline.txt; dig +short kalebnim.dev A | tee /tmp/04-zone-apex-baseline.txt; echo "DOMAIN_ATTACHED_TARGET_READ_OK"</automated>
  </verify>
  <acceptance_criteria>
    - The pre-change baseline for `kalebnim.dev` NS, apex A, `www`, and the target subdomain CNAME is recorded verbatim in the SUMMARY, captured BEFORE `vercel domains add` ran
    - `dig +short overlay-notes.kalebnim.dev CNAME` produced empty output in the baseline (nothing pre-existing to disturb)
    - `vercel domains ls` (or `vercel domains inspect overlay-notes.kalebnim.dev`) lists `overlay-notes.kalebnim.dev` associated with the `overlay-notes-landing` project
    - The extracted CNAME target is recorded verbatim in the SUMMARY, matches the pattern `vercel-dns`, and is NOT the literal generic value `cname.vercel-dns.com`
    - The SUMMARY states explicitly whether a TXT verification record was requested, and records its exact name and value if so
    - No `gcloud` command was executed and no `vercel certs` command was executed
  </acceptance_criteria>
  <done>The domain is attached to the Vercel project, the project-specific CNAME target (and any TXT record) has been read live and recorded verbatim, and the shared zone's pre-change state is captured for later comparison.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: HUMAN — add the CNAME (and any TXT) record in the Google Cloud DNS console</name>
  <files>(none — the change lives entirely in Google Cloud DNS, outside this repo)</files>
  <read_first>
    - @.planning/phases/04-deployment-domain-verification/04-RESEARCH.md — `## Common Pitfalls` 3, `## Environment Availability` (the gcloud row), `## Don't Hand-Roll` (the DNS-automation row)
    - Task 1's recorded output in this plan's SUMMARY draft — the exact CNAME target string to hand to the human
  </read_first>
  <what-built>
    Task 1 attached `overlay-notes.kalebnim.dev` to the Vercel project and read the exact,
    project-specific CNAME target out of `vercel domains inspect`. Everything that can be automated
    has been. The DNS record itself cannot be: `kalebnim.dev`'s real nameservers are Google Cloud
    DNS (`ns-cloud-b1..b4.googledomains.com`), and the local `gcloud` CLI is authenticated but pinned
    to impersonate a deleted service account belonging to an unrelated GCP project (`til-ai-2026`),
    so every `gcloud dns` call fails with `NOT_FOUND`. 04-RESEARCH.md verified this live and rules
    repairing that config out of scope. This is a genuine human-only step.
  </what-built>
  <how-to-verify>
    1. Open the Google Cloud Console with the account that already manages the `kalebnim.dev` zone
       (the same account that owns its existing apex A record).
    2. Go to **Network Services → Cloud DNS**, and open the managed zone for **kalebnim.dev**.
    3. Click **Add standard** / **Add record set** and create exactly one record:
       - **DNS name:** `overlay-notes` (the console appends `.kalebnim.dev.` for you — do NOT type
         the full FQDN if the console already shows the zone suffix)
       - **Resource record type:** `CNAME`
       - **TTL:** leave the console default (300s / 5 minutes is fine; a shorter TTL only speeds up
         a future change)
       - **Canonical name / data:** paste the exact target string recorded in Task 1 and printed in
         the executor's checkpoint message. Do **not** type `cname.vercel-dns.com` — that generic
         value is wrong for this project and produces a domain that never verifies.
    4. **Only if the executor's message says a TXT record was requested**, add that second record in
       the same visit, copying its name and value verbatim from the message.
    5. **Do not change anything else in this zone.** The apex `A` record and the `NS` record set
       serve a different live site from a different Vercel project. Add records; do not edit or
       delete any.
    6. Save, and tell the executor you are done.
  </how-to-verify>
  <action>
    The executor's job around this checkpoint is to make the human's step unambiguous and then to
    confirm it by polling, not by asking again.

    Before pausing, print a checkpoint message containing: the literal record name `overlay-notes`,
    the literal type `CNAME`, and the exact target string captured in Task 1, quoted on its own line
    so it can be copied without ambiguity — plus the TXT record's name and value if and only if Task
    1 recorded that Vercel requested one. State explicitly in the message that nothing else in the
    zone may be edited, and name the apex `A` record and the `NS` set as the records to leave alone.
    Do not paste a placeholder or an example value; if Task 1 did not produce a concrete target, stop
    and re-run `vercel domains inspect overlay-notes.kalebnim.dev` instead of pausing with a guess.

    After the resume signal, poll the `<post-condition>` checks below with backoff for up to about ten
    minutes before treating a non-result as a failure. Distinguish the two failure shapes: an empty
    `dig` result is propagation delay and warrants continued polling; a `dig` result that differs from
    Task 1's recorded target is a wrong value and warrants showing the human both strings side by
    side immediately rather than waiting longer.

    Do not attempt any `gcloud` command as a shortcut while waiting — 04-RESEARCH.md verified the
    local gcloud auth is pinned to a deleted service account in an unrelated project, and repairing
    it is out of scope.
  </action>
  <resume-signal>Type "added" once the record(s) are saved, or describe what the console showed if you could not complete it (for example: no permission on the zone, or the record name collides).</resume-signal>
  <verify>
    <automated>for i in $(seq 1 20); do dig +short overlay-notes.kalebnim.dev CNAME | grep -qE 'vercel-dns' && break; sleep 30; done; dig +short overlay-notes.kalebnim.dev CNAME | grep -qE 'vercel-dns' || { echo "FAIL: CNAME did not resolve after polling"; exit 1; }; dig +short kalebnim.dev NS | sort | diff -q - /tmp/04-zone-ns-baseline.txt && dig +short kalebnim.dev A | diff -q - /tmp/04-zone-apex-baseline.txt && vercel domains inspect overlay-notes.kalebnim.dev 2>&1 | grep -qiv 'invalid configuration' && echo "CNAME_LIVE_ZONE_INTACT_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `dig +short overlay-notes.kalebnim.dev CNAME` returns the exact target string recorded in Task 1, with the trailing dot normalised
    - `vercel domains inspect overlay-notes.kalebnim.dev` no longer reports a missing or invalid configuration for the subdomain
    - `dig +short kalebnim.dev NS | sort` is byte-identical to the Task 1 baseline
    - `dig +short kalebnim.dev A` is byte-identical to the Task 1 baseline
    - The checkpoint message shown to the human contained the concrete target string from Task 1, not a placeholder or Vercel's documentation example
    - No `gcloud` command was executed at any point
  </acceptance_criteria>
  <done>The human has added the additive CNAME (and any requested TXT) record in the Google Cloud DNS console, `dig` resolves the subdomain to exactly the target read from Vercel, and the zone's pre-existing NS and apex A records are provably unchanged.</done>
  <post-condition>
    The executor polls for these rather than asking again — all machine-checkable:
    - `dig +short overlay-notes.kalebnim.dev CNAME` returns the exact target recorded in Task 1
      (normalising the trailing dot). Poll with backoff for up to ~10 minutes before treating a
      non-result as a failure; propagation delay is expected, a wrong value is not.
    - `vercel domains inspect overlay-notes.kalebnim.dev` stops reporting a missing/invalid
      configuration for the subdomain.
    - `dig +short kalebnim.dev A` and `dig +short kalebnim.dev NS | sort` are byte-identical to the
      baselines captured in Task 1 — proving the change was additive.
    If `dig` returns a value that differs from Task 1's recorded target, the cause is almost always a
    mistyped or guessed CNAME value: show the human both strings side by side and ask for a
    correction rather than waiting longer.
  </post-condition>
</task>

<task type="auto">
  <name>Task 3: Verify HTTPS, certificate validity, absent robots header, and an unharmed zone on the live domain</name>
  <files>(no repo files — verification only)</files>
  <read_first>
    - @scripts/verify-deployment.sh (created in 04-01 — the assertion set run here with no flags)
    - @next.config.ts (the production branch of `headers()` — this task makes the first real-domain observation of it)
    - @.planning/phases/03-seo-metadata-structured-data-social-card/03-VERIFICATION.md — `### Deferred Items` row 1, the SEO-08 debt this task closes
    - @.planning/phases/04-deployment-domain-verification/04-RESEARCH.md — `## Recommended Command Sequence` step 8, `## Code Examples` (the two `curl -sD-` header checks), `## Security Domain` (V9 Communications)
    - Task 1's baseline `dig` output recorded in the SUMMARY
  </read_first>
  <action>
    (a) DNS + Vercel agreement. Run `dig +short overlay-notes.kalebnim.dev CNAME` and confirm it
    matches Task 1's recorded target byte-for-byte after normalising the trailing dot. Run
    `vercel domains inspect overlay-notes.kalebnim.dev` and confirm it reports a valid
    configuration for `overlay-notes-landing`.

    (b) TRANSPORT (ASVS V9). Confirm TLS is real, not just that a 200 came back: run
    `curl -sSv https://overlay-notes.kalebnim.dev/ -o /dev/null` and confirm from the handshake output
    that the certificate is valid and its subject or SAN covers `overlay-notes.kalebnim.dev`. Then
    confirm `curl -sSI http://overlay-notes.kalebnim.dev/` redirects to the https origin (a 3xx with
    an https `location`) rather than serving plaintext. Do not pass `--insecure` anywhere.

    (c) SEO-08 CLOSURE (Success Criterion #3). Run
    `curl -sSD - -o /dev/null https://overlay-notes.kalebnim.dev/` and confirm there is no robots
    response header in the output at all — this is the live production observation Phase 3 explicitly
    deferred into Phase 4 (03-VERIFICATION.md Deferred Items row 1). Record the full response header
    block verbatim in the SUMMARY as the evidence, not a paraphrase. If the header IS present, the
    cause is almost certainly Vercel's "Automatically expose System Environment Variables" being off
    for this project, which would leave `VERCEL_ENV` undefined at build time: report that diagnosis
    and stop. Do not edit `next.config.ts` to work around it.

    (d) FULL SURFACE. Run `bash scripts/verify-deployment.sh https://overlay-notes.kalebnim.dev` with
    no flags and require exit 0 — this re-asserts the crawl surfaces, OG image, JSON-LD and canonical
    on the custom domain, and includes the absent-robots-header assertion from (c) as a re-runnable
    gate rather than a one-off command log.

    (e) ZONE INTEGRITY. Re-run `dig +short kalebnim.dev A` and `dig +short kalebnim.dev NS | sort`
    and diff them against Task 1's baselines; they must be identical. Also confirm
    `curl -sS -o /dev/null -w '%{http_code}' https://kalebnim.dev/` still returns a success or
    redirect code — the sibling apex site must be unaffected by this phase.
  </action>
  <verify>
    <automated>dig +short overlay-notes.kalebnim.dev CNAME | grep -qE 'vercel-dns' && curl -sS -o /dev/null -w '%{http_code}\n' https://overlay-notes.kalebnim.dev/ | grep -qx 200 && [ "$(curl -sSD - -o /dev/null https://overlay-notes.kalebnim.dev/ | grep -ci '^x-robots-tag')" = "0" ] && bash scripts/verify-deployment.sh https://overlay-notes.kalebnim.dev && dig +short kalebnim.dev NS | sort | diff -q - /tmp/04-zone-ns-baseline.txt && dig +short kalebnim.dev A | diff -q - /tmp/04-zone-apex-baseline.txt && echo "DOMAIN_LIVE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `dig +short overlay-notes.kalebnim.dev CNAME` returns the exact target recorded in Task 1 (trailing dot normalised)
    - `vercel domains inspect overlay-notes.kalebnim.dev` reports a valid configuration for the `overlay-notes-landing` project
    - `curl -sS -o /dev/null -w '%{http_code}' https://overlay-notes.kalebnim.dev/` returns `200`
    - `curl -sSv https://overlay-notes.kalebnim.dev/ -o /dev/null` completes the TLS handshake with a valid certificate covering `overlay-notes.kalebnim.dev`, with no `--insecure` flag anywhere in the command
    - `curl -sSI http://overlay-notes.kalebnim.dev/` returns a 3xx whose `location` is the `https://` origin
    - `curl -sSD - -o /dev/null https://overlay-notes.kalebnim.dev/ | grep -ci '^x-robots-tag'` returns `0`, and the full verbatim response header block is recorded in the SUMMARY as SEO-08 closure evidence
    - `bash scripts/verify-deployment.sh https://overlay-notes.kalebnim.dev` exits 0
    - `dig +short kalebnim.dev NS | sort` and `dig +short kalebnim.dev A` are byte-identical to the Task 1 baselines
    - `curl -sS -o /dev/null -w '%{http_code}' https://kalebnim.dev/` returns a 2xx or 3xx — the sibling apex site is unaffected
  </acceptance_criteria>
  <done>The custom domain resolves to the correct project-specific target, serves the site over valid TLS with no robots header, passes the full deployed-surface verifier, and the shared DNS zone is provably unchanged apart from the added record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared DNS zone → two independent Vercel projects | The `kalebnim.dev` zone already serves the author's apex site from a different project. A human editing this zone by hand can affect a site outside this phase's scope. |
| public internet → the new subdomain over TLS | Once the CNAME resolves, any client can reach the origin; the transport must be genuinely encrypted with a certificate that actually covers this hostname. |
| Vercel project ownership → DNS record | The record's validity depends on the Vercel project continuing to exist under the same name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | Human console edit of the shared `kalebnim.dev` Cloud DNS zone | high | mitigate | Task 1 captures NS / apex A / www / target-CNAME baselines BEFORE any change; the checkpoint instruction is explicitly additive-only and names the apex as off-limits; Task 3 diffs the post-change `dig` output against those baselines and re-checks that `https://kalebnim.dev/` still responds. A destructive edit fails the plan loudly instead of going unnoticed. |
| T-04-06 | Spoofing | Dangling CNAME → subdomain takeover if the `overlay-notes-landing` Vercel project is ever deleted or renamed while the record still points at it | medium | accept | Not introduced by this phase — the project is being created, not removed. Recorded here and in the SUMMARY as a maintenance constraint for whoever ever decommissions the site: remove the DNS record first, then the Vercel project. No code change mitigates this. |
| T-04-07 | Information Disclosure | TLS not actually terminating, or a certificate that does not cover this hostname, on first resolve | medium | mitigate | Task 3(b) inspects the handshake and certificate subject/SAN explicitly rather than accepting a bare 200, forbids `--insecure`, and separately asserts that plain HTTP redirects to HTTPS instead of serving cleartext (ASVS V9). |
| T-04-08 | Spoofing | Entering a guessed or stale CNAME value (`cname.vercel-dns.com`) instead of the project-specific target | medium | mitigate | The target is read live from `vercel domains inspect` in Task 1, the generic value is treated as a read failure by the task's own verify command, and Task 3 asserts the resolved value matches the recorded string byte-for-byte. |

_ASVS L1, block on high: T-04-05 is the one high-severity threat and is dispositioned `mitigate` with a before/after baseline diff over the shared zone._
</threat_model>

<verification>
- `dig +short overlay-notes.kalebnim.dev CNAME` matches the target read live in Task 1; it is not the generic `cname.vercel-dns.com`.
- `vercel domains inspect overlay-notes.kalebnim.dev` reports a valid configuration for `overlay-notes-landing`.
- `https://overlay-notes.kalebnim.dev/` returns 200 over a valid, hostname-matching certificate; plain HTTP redirects to HTTPS.
- The live response carries no robots header — SEO-08's deferred production observation is closed, with the verbatim header block recorded as evidence.
- `bash scripts/verify-deployment.sh https://overlay-notes.kalebnim.dev` exits 0.
- `kalebnim.dev`'s NS and apex A records are byte-identical to the pre-change baseline, and the apex site still responds.
</verification>

<success_criteria>
- DEPL-03 is satisfied: the site serves on `https://overlay-notes.kalebnim.dev` over HTTPS.
- ROADMAP Success Criterion #3 is satisfied: after the human added the CNAME at Google Cloud DNS, the site serves over HTTPS with no leftover robots header.
- Phase 3's deferred SEO-08 production-header item is closed with a live, recorded observation on the real domain.
- The shared `kalebnim.dev` zone is provably unchanged except for the additive record.
</success_criteria>

<output>
Create `.planning/phases/04-deployment-domain-verification/04-02-SUMMARY.md` when done. It MUST
record: the exact CNAME target string, whether a TXT record was requested (and its value if so),
the pre- and post-change `dig` baselines, the verbatim response header block from the live domain,
and the dangling-CNAME maintenance constraint from T-04-06.
</output>
