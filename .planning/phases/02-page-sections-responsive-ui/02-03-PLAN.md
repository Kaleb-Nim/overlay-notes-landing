---
phase: 02-page-sections-responsive-ui
plan: 3
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - tests/landing.spec.ts
autonomous: true
requirements: [PAGE-10, PAGE-11, PAGE-13, QUAL-01, QUAL-05, QUAL-06, ASSET-03]

must_haves:
  truths:
    - "scripts/test-gate.sh runs the real Playwright suite (no longer self-skipping) and exits 0 across the desktop, tablet, and mobile projects"
    - "tests/landing.spec.ts is EXTENDED with per-section presence checks and the locked-URL / invariant assertions are left unchanged (never weakened)"
    - "tests/seo.spec.ts still self-guards and skips gracefully (Phase 3 work not present), so it does not block the Phase 2 gate"
    - "bun run build stays green and bun scripts/verify-claims.ts prints PASS"
  artifacts:
    - "tests/landing.spec.ts extended with per-section presence assertions"
  key_links:
    - "Installed Playwright (Plan 02-01) + built sections (Plan 02-02) -> live green test gate"
    - "Per-section id presence assertions -> the section ids rendered by app/page.tsx (#who, #features, #how-its-different, #origin-story, #faq, #support, footer)"
---

<objective>
Lock in Phase 2's behavioral evidence: confirm the live Playwright gate is green across all three viewports, then EXTEND tests/landing.spec.ts with per-section presence checks so future markup changes can't silently drop a section — without weakening any of the locked-URL or invariant assertions. Close the phase by confirming build, gate, and claims all pass together.

Purpose: The gate must be provably LIVE (running, not skipping) and GREEN, and the suite must cover section presence, not just the locked contract. This is the phase's exit criterion.
Output: extended tests/landing.spec.ts; confirmed green gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/02-page-sections-responsive-ui/02-UI-SPEC.md
@tests/landing.spec.ts
@tests/seo.spec.ts
@scripts/test-gate.sh
@playwright.config.ts
@TESTING.md
@scripts/verify-claims.ts
@app/page.tsx
</context>

## Discipline (applies to the whole plan)

- This plan owns tests/landing.spec.ts ONLY. If a locked assertion genuinely fails, the fix belongs in app/page.tsx (Plan 02-02 scope) or app/globals.css (Plan 02-01 scope) — do NOT weaken the test to make it pass. Since Plan 02-02's verify already ran the suite green, no source change should be needed here.
- NEVER modify the WEBSTORE / REPO / SUPPORT / PRIVACY constants or the existing test bodies. Only ADD new `test(...)` blocks (per-section presence). TESTING.md and CLAUDE.md forbid weakening the locked-URL / invariant assertions.
- Keep test names as behavior descriptions, not "screenshot"-style placeholders.

<tasks>

<task type="auto">
  <name>Task 1: Confirm the live gate is running and green across all viewports</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - @scripts/test-gate.sh (confirm it now reaches the real `bunx playwright test` branch)
    - @playwright.config.ts (the three projects: desktop 1280, tablet 760, mobile 360)
    - @tests/landing.spec.ts (the assertions that must pass)
    - @tests/seo.spec.ts (must still self-skip during Phase 2)
  </read_first>
  <action>
    Run `bash scripts/test-gate.sh` and confirm two things: (1) it prints the "running Playwright suite" line — i.e. it is LIVE, not emitting any "skipping" message — proving Plan 02-01's install took effect; and (2) it exits 0 with every tests/landing.spec.ts test passing across the desktop, tablet, and mobile projects (the 360px no-horizontal-scroll check is the one most likely to catch a regression). Also confirm tests/seo.spec.ts tests SKIP (not fail) — they self-guard until Phase 3 metadata exists. If anything in landing.spec.ts fails, stop and record which source file (app/page.tsx or app/globals.css) needs the fix rather than editing the test; the expectation is that Plan 02-02 already left the suite green, so this task is a confirmation, not a repair.
  </action>
  <acceptance_criteria>
    - `bash scripts/test-gate.sh` output contains "running Playwright suite" and contains no "skipping" line (gate is LIVE)
    - The command exits 0; all tests/landing.spec.ts tests pass on desktop, tablet, and mobile
    - tests/seo.spec.ts tests are reported as skipped (self-guarding), not failed
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/test-gate.sh 2>&1 | tee /tmp/gate.out; grep -q "running Playwright suite" /tmp/gate.out && ! grep -qi "skipping" /tmp/gate.out</automated>
  </verify>
  <done>The gate is confirmed live and green across all three viewports; seo.spec.ts skips gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Extend landing.spec.ts with per-section presence checks</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - @tests/landing.spec.ts (extend this — do not weaken; note the existing describe blocks and locked constants)
    - @.planning/phases/02-page-sections-responsive-ui/02-UI-SPEC.md (Section Inventory & Anchor IDs table — the authoritative id list to assert)
    - @app/page.tsx (confirm the actual rendered ids/structure the new assertions target)
  </read_first>
  <action>
    Add a new `test.describe('Section presence', ...)` block (and/or additional tests) to tests/landing.spec.ts that asserts each of the ten sections is on the page — WITHOUT touching any existing test or constant. Assert the presence of the sections that own an id: `#who`, `#features`, `#how-its-different`, `#origin-story`, `#faq`, `#support`, and the `<footer>`. Assert the hero screenshot image is visible (an img whose alt describes the CS2030 lecture-notes annotation, i.e. a non-empty alt that is not the bare word for a screen capture). Assert the FAQ section renders six question entries (e.g. the `.diff .h` count within `#faq` is 6, matching faqs.length). Assert the "Who it's for" chips (4) and the feature cards (4) are present. Assert the support tip amounts are non-interactive — i.e. the elements showing "$3"/"$8"/"$20" are not anchors (reuse/extend the existing dead-link invariant rather than duplicating it). Keep every assertion keyed to structural facts (ids, counts, roles), and keep test names descriptive of the behavior. Re-run the suite to confirm the additions pass across all viewports and that the pre-existing tests are byte-for-byte unchanged.
  </action>
  <acceptance_criteria>
    - tests/landing.spec.ts contains a new "Section presence" describe (or equivalent added tests) asserting `#who`, `#features`, `#how-its-different`, `#origin-story`, `#faq`, `#support`, and footer exist
    - A new assertion confirms the FAQ renders 6 question entries and the who-chips/feature-cards counts
    - The WEBSTORE / REPO / SUPPORT / PRIVACY constants and all pre-existing test bodies are unchanged (only additions)
    - `bash scripts/test-gate.sh` exits 0 across desktop/tablet/mobile with the new tests passing
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Section presence" tests/landing.spec.ts && grep -Fq "ogekdbffoapphpabjphfgeppildcleck" tests/landing.spec.ts && grep -Fq "buymeacoffee.com/kaleb-nim" tests/landing.spec.ts && bash scripts/test-gate.sh</automated>
  </verify>
  <done>landing.spec.ts is extended with per-section presence coverage; locked constants/assertions intact; suite green across all viewports.</done>
</task>

<task type="auto">
  <name>Task 3: Final phase verification — build, gate, and claims together</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - @scripts/verify-claims.ts (the claim-integrity checks that must still PASS after copy wiring)
    - @TESTING.md (how the gate feeds the verifier as behavioral evidence)
  </read_first>
  <action>
    Run the three phase-closing checks in sequence and confirm all pass together: `bun run build` (production build compiles), `bun scripts/verify-claims.ts` (banned-term scan clean, CONT-03 privacy statement intact, faqs.length 6, baseUrl exact — confirms no banned copy crept in during Plan 02-02's wiring), and `bash scripts/test-gate.sh` (the live Playwright suite exits 0 across desktop/tablet/mobile). Record in the SUMMARY that the E2E gate is now LIVE (running the real suite, no skip) and GREEN — this is the acceptance evidence that Phase 2's success criteria are machine-verified rather than deferred to human sign-off.
  </action>
  <acceptance_criteria>
    - `bun run build` exits 0
    - `bun scripts/verify-claims.ts` prints "PASS" and exits 0
    - `bash scripts/test-gate.sh` runs the real suite (prints "running Playwright suite"), exits 0, and reports zero failures across desktop/tablet/mobile
    - SUMMARY notes the gate is LIVE + GREEN as behavioral evidence for the phase
  </acceptance_criteria>
  <verify>
    <automated>bun run build && bun scripts/verify-claims.ts && bash scripts/test-gate.sh</automated>
  </verify>
  <done>Build green, claims PASS, live Playwright gate green across all viewports — Phase 2 exit criteria met with machine evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> locked contract | the E2E suite is the enforcement point for the phase's locked URLs and invariants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | tests/landing.spec.ts locked assertions | medium | mitigate | Extend-only discipline: the WEBSTORE/REPO/SUPPORT/PRIVACY constants and existing test bodies must remain unchanged; Task 2's verify greps for the locked URL literals to prove they still exist in the file. A weakened assertion would let a wrong destination or dropped section pass silently. |
| T-02-06 | Repudiation | gate falsely reported green | low | mitigate | Task 1/3 assert the gate prints "running Playwright suite" and contains no "skipping" line, so a self-skipped (vacuously green) gate cannot be mistaken for real evidence. |
</threat_model>

## Artifacts this phase produces

This plan (02-03) produces the extended `tests/landing.spec.ts` (per-section presence coverage on top of the locked contract) and the confirmed-green live test gate that credits Phase 2's success criteria as machine-verified. It depends on Plan 02-01 (installed Playwright) and Plan 02-02 (built sections).

<verification>
- `bash scripts/test-gate.sh` prints "running Playwright suite" (LIVE), exits 0, zero failures across desktop/tablet/mobile.
- tests/landing.spec.ts extended with per-section presence checks; locked constants/assertions unchanged.
- tests/seo.spec.ts still self-skips (Phase 3 not present).
- `bun run build` green; `bun scripts/verify-claims.ts` PASS.
</verification>

<success_criteria>
The live E2E gate is green across all three viewports with per-section presence coverage added and no locked assertion weakened; build and claims checks pass — Phase 2 is machine-verified complete.
</success_criteria>

<output>
Create `.planning/phases/02-page-sections-responsive-ui/02-03-SUMMARY.md` when done.
</output>
