---
phase: 03-seo-metadata-structured-data-social-card
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified:
  - tests/seo.spec.ts
autonomous: true
requirements: [SEO-01, SEO-02, SEO-03, SEO-04, SEO-05, SEO-06, SEO-07, SEO-08, SEO-09, ASSET-01, ASSET-02]

must_haves:
  truths:
    - "tests/seo.spec.ts ASSERTS (never silently skips) every SEO surface now that plans 03-01/03-02 built them: title ≤60, description ≤160, lang=en, absolute canonical + og:image on overlay-notes.kalebnim.dev, no old GitHub Pages host in <head>, SoftwareApplication JSON-LD without aggregateRating/review/FAQPage, robots.txt 200 + sitemap pointer, sitemap.xml 200."
    - "New assertions added: og:description is present and DISTINCT from the meta description; twitter:card = summary_large_image with NO twitter:site/twitter:creator; the og:image:width/height meta = 1200/630 AND public/og-image.png is exactly 1200×630 on disk; X-Robots-Tag: noindex is present on the local (non-production) test run."
    - "The full gate `bash scripts/test-gate.sh` passes green with ZERO skipped SEO tests."
    - "tests/landing.spec.ts locked assertions (CTA URLs, alt-text, no-dead-links, responsive, reduced-motion, focus, nav-anchor) remain unweakened — extend, never delete."
  artifacts:
    - tests/seo.spec.ts (extended — self-guards flipped to live assertions + 4 new checks)
  key_links:
    - "This spec is the durable regression gate that credits SEO-01..09 + ASSET-01 as machine-VERIFIED (workflow.verifier=true), shrinking the end-of-phase human UAT surface."
  backstops:
    - statement: "The X-Robots-Tag test asserts PRESENCE only (local/CI runs never set VERCEL_ENV=production, so the header is always present there by design). Do NOT add an assertion expecting the header to be ABSENT under the normal local gate — it would falsely fail every run. Production absence is a Phase 4 curl -I checkpoint."
      verification: backstop
    - statement: "SEO-05 Rich Results Test (Google) and ASSET-02 (LinkedIn Post Inspector) are external tools requiring, respectively, a browser and the live deployed URL. The spec verifies JSON-LD structural correctness and the OG file/tag; the external-tool passes remain a recommended human check (Rich Results now; LinkedIn in Phase 4)."
      verification: human_needed
---

## Phase Goal

**As a** maintainer running the autonomous test gate, **I want to** have every SEO and
social-card guarantee asserted by a live Playwright suite (not silently skipped), **so that**
a future regression that breaks the title, canonical, JSON-LD, robots/sitemap, or OG card is
caught automatically instead of shipping unnoticed.

_(Derived faithfully from ROADMAP.md Phase 3 Goal + TESTING.md's "confirm none silently skip" mandate — no scope invented.)_

<objective>
Flip the pre-existing self-guarding `tests/seo.spec.ts` from skipping to asserting now that
plans 03-01 and 03-02 have built the metadata, JSON-LD, robots/sitemap, header, and OG image;
add the four assertions CONTEXT.md/PATTERNS.md call for; and confirm the full gate runs green
with zero silent skips and without weakening `tests/landing.spec.ts`.

This is the Wave-2 verification slice — it converts the built surfaces into durable,
machine-credited regression coverage. It depends on both Wave-1 plans and owns the shared
`tests/seo.spec.ts` file exclusively (no other Phase 3 plan edits it).

Purpose: Make the phase's success criteria machine-VERIFIED and regression-proof.
Output: Extended `tests/seo.spec.ts`; a green `bash scripts/test-gate.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo-metadata-structured-data-social-card/03-CONTEXT.md
@.planning/phases/03-seo-metadata-structured-data-social-card/03-PATTERNS.md
@tests/seo.spec.ts
@tests/landing.spec.ts
@TESTING.md
@playwright.config.ts
@scripts/test-gate.sh
</context>

<artifacts>
## Artifacts This Plan Produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `tests/seo.spec.ts` | modify | Existing self-guarded assertions now fire (their skip guards become dead once the features exist); adds 4 new assertions — og:description distinct, twitter card + absent handles, og:image dimensions (meta + on-disk file), X-Robots-Tag present locally. No existing locked assertion is removed or weakened. |
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/seo.spec.ts — un-skip existing checks and add the four new assertions</name>
  <files>tests/seo.spec.ts</files>
  <read_first>
    - @tests/seo.spec.ts (full current suite — the self-guarding title/description/lang/canonical/og:image/no-stale-host/JSON-LD/robots/sitemap tests; extend in place, ADD assertions, keep the locked ones)
    - @.planning/phases/03-seo-metadata-structured-data-social-card/03-PATTERNS.md (tests/seo.spec.ts section, lines 166-174 — the exact list of assertions to ADD and the "never weaken" rule)
    - @.planning/phases/03-seo-metadata-structured-data-social-card/03-CONTEXT.md (locked values the assertions check: distinct og:description, twitter summary_large_image with no handle, og-image 1200×630)
    - @.planning/phases/03-seo-metadata-structured-data-social-card/03-RESEARCH.md (Common Pitfalls #2 — assert the X-Robots-Tag is PRESENT locally, never absent)
  </read_first>
  <action>
    Extend the existing SEO spec so its self-guards no longer trigger (the built features make the guards dead) and add these four assertions (ADD — never replace or weaken the locked canonical/JSON-LD/no-stale-host/no-FAQPage checks):

    1. og:description present and DISTINCT from the meta description: read `meta[property="og:description"]` and `meta[name="description"]` content; assert both are non-empty and not equal.
    2. Twitter card: assert `meta[name="twitter:card"]` content = 'summary_large_image', and that `meta[name="twitter:site"]` and `meta[name="twitter:creator"]` each have count 0 (no handle configured).
    3. OG image dimensions: assert `meta[property="og:image:width"]` = '1200' and `meta[property="og:image:height"]` = '630'; ALSO read `public/og-image.png` from disk in the Node test context and assert its PNG IHDR width/height are exactly 1200×630.
    4. Preview-noindex header: request `/` and assert the `X-Robots-Tag` response header contains 'noindex' (the local test run never sets VERCEL_ENV=production, so the header is always present here by design). Add a comment that its ABSENCE on production is a Phase 4 curl check — do NOT assert absence locally.

    Preserve every existing locked assertion verbatim (absolute-canonical regex, og:image absolute regex, no old GitHub Pages host in `<head>`, SoftwareApplication present, no aggregateRating, no review, no FAQPage, robots.txt 200 + sitemap pointer, sitemap.xml 200). Keep the `desktop`-only guard for the head/JSON-LD checks. Use Node `fs` for the on-disk PNG read (Playwright tests run under Node; the repo has no image lib — parse the IHDR bytes directly).
  </action>
  <verify>
    <automated>bunx playwright test tests/seo.spec.ts --project=desktop --reporter=line 2>&1 | tee /tmp/seo-run.txt; grep -Eq "skipped|did not run" /tmp/seo-run.txt && { echo "FAIL: SEO tests skipped"; exit 1; }; grep -q "passed" /tmp/seo-run.txt && echo "SEO_SPEC_LIVE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Running `tests/seo.spec.ts` on the `desktop` project passes with ZERO skipped tests (no self-guard fires — every SEO feature now exists).
    - The four new assertions are present and pass: og:description distinct from meta description; twitter:card summary_large_image with no site/creator; og:image:width/height meta = 1200/630 AND `public/og-image.png` IHDR = 1200×630; X-Robots-Tag present (contains 'noindex') on the local run.
    - No existing locked assertion is deleted or loosened (canonical/JSON-LD/no-stale-host/no-FAQPage/robots/sitemap all intact).
  </acceptance_criteria>
  <done>tests/seo.spec.ts asserts every SEO surface with zero skips on desktop, and the four new checks (distinct og:description, twitter card + no handle, og:image 1200×630 meta + file, X-Robots-Tag present) pass.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full green gate + build; confirm no silent skips and landing.spec.ts unweakened</name>
  <files>tests/seo.spec.ts</files>
  <read_first>
    - @scripts/test-gate.sh (the real gate: `bunx playwright test` across all projects)
    - @tests/landing.spec.ts (the Phase 2 locked suite — must remain unchanged/unweakened)
    - @TESTING.md ("Extend, don't weaken"; the executor must confirm none of the SEO tests silently skip)
  </read_first>
  <action>
    Run the full autonomous gate `bash scripts/test-gate.sh` and confirm it exits 0 (green) across the desktop/tablet/mobile projects, with the SEO suite asserting (not skipping). Run `bun run build` and confirm it exits 0. Confirm `tests/landing.spec.ts` was not modified in this phase (no locked assertion removed) — this task only finalizes; it does not touch the landing spec.

    Record in the SUMMARY the carry-forward items for Phase 4: (a) the SEO-08 production-header-absence `curl -I` checkpoint (VERCEL_ENV / Vercel System-Env toggle), and (b) ASSET-02 LinkedIn Post Inspector live render against the deployed URL. Note the recommended (non-blocking) SEO-05 Google Rich Results Test human check against the built HTML.
  </action>
  <verify>
    <automated>bash scripts/test-gate.sh 2>&1 | tail -20; STATUS=${PIPESTATUS[0]:-$?}; [ "$STATUS" = "0" ] || exit 1; bun run build 2>&1 | tail -5; git diff --name-only origin/main..HEAD -- tests/landing.spec.ts 2>/dev/null; echo "GATE_GREEN_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/test-gate.sh` exits 0 with the full Playwright suite green (SEO + landing), zero silently-skipped SEO tests.
    - `bun run build` exits 0.
    - `tests/landing.spec.ts` is unchanged by this phase (its locked CTA/alt/responsive/reduced-motion/focus/nav assertions are intact).
    - The SUMMARY records the Phase 4 carry-forwards (SEO-08 production curl -I; ASSET-02 LinkedIn) and the recommended SEO-05 Rich Results human check.
  </acceptance_criteria>
  <done>`bash scripts/test-gate.sh` is green across all viewports, `bun run build` passes, tests/landing.spec.ts is unweakened, and the Phase 4 carry-forwards are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new (test-only) | This plan extends the existing Playwright spec and introduces no runtime/production surface. Tests execute only in the local/CI gate against a dev server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | `tests/seo.spec.ts` | low | accept | Test code runs only against the local/CI dev server; it ships no production surface. "Extend, don't weaken" discipline preserves the locked invariants (canonical/JSON-LD/no-stale-host/no-FAQPage), so the regression gate cannot be silently loosened. |

_ASVS L1: no high-severity threats — test-only changes, no new dependencies, no runtime surface._
</threat_model>

<verification>
- `tests/seo.spec.ts` asserts every SEO surface with zero skips on the `desktop` project.
- The four new assertions (og:description distinct, twitter card + no handle, og:image 1200×630 meta + file, X-Robots-Tag present) pass.
- `bash scripts/test-gate.sh` is green across all viewports; `bun run build` exits 0.
- `tests/landing.spec.ts` is unweakened.
- Carry-forwards recorded for Phase 4 (SEO-08 production curl; ASSET-02 LinkedIn); SEO-05 Rich Results recommended as a human check.
</verification>

<success_criteria>
- Every Phase 3 SEO/ASSET success criterion has durable, machine-credited Playwright coverage.
- The full gate passes green with no silently-skipped SEO tests, and the Phase 2 locked suite remains intact.
</success_criteria>

<output>
Create `.planning/phases/03-seo-metadata-structured-data-social-card/03-03-SUMMARY.md` when done.
</output>
