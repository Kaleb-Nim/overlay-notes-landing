---
phase: 03-seo-metadata-structured-data-social-card
plan: 02
subsystem: seo
tags: [playwright, chromium, og-image, social-card, bun, next-font, static-asset]

# Dependency graph
requires:
  - phase: 03-seo-metadata-structured-data-social-card (plan 01)
    provides: "app/layout.tsx og:image meta tag pointing at /og-image.png (this plan produces that file)"
provides:
  - "public/og-image.png — committed 1200x630 OG social card"
  - "scripts/generate-og-image.ts — reproducible Bun+Playwright generation script"
  - "scripts/og-image-assets/{ShantellSans-700,PublicSans-600}.ttf — committed fonts for standalone-script self-hosting"
  - "package.json generate:og script"
affects: [phase-4-deployment-linkedin-post-inspector-verification]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Standalone Bun script drives @playwright/test's bundled-Chromium chromium export to screenshot a self-contained base64-inlined HTML string (fonts + source image) at an exact viewport size — zero network dependency, deterministic, reproducible on every run"
    - "Font self-hosting for scripts outside the Next.js build: download once from Google Fonts CDN, commit the .ttf, embed via base64 @font-face — next/font's output is not reusable outside the Next.js compile pipeline"
    - "CSS 'cover' scale math (max of width-ratio/height-ratio) for cropping a source image into a fixed box via background-size/background-position, computed from named consts for easy re-tuning"

key-files:
  created:
    - scripts/og-image-assets/ShantellSans-700.ttf
    - scripts/og-image-assets/PublicSans-600.ttf
    - scripts/generate-og-image.ts
    - public/og-image.png
  modified:
    - package.json

key-decisions:
  - "Used the UI-SPEC's recommended default crop rect (x:400,y:170,w:780,h:630) as-is — visual QA confirmed the annotation cluster is legible and not awkwardly cut, no tuning needed"
  - "Computed background-size/background-position at script-runtime from named CROP_*/PHOTO_BOX_* consts (cover-scale formula) rather than hardcoding the final pixel values, so a future crop adjustment is a one-line const change"

patterns-established:
  - "Pattern: Bun scripts under scripts/ that need self-hosted fonts read committed .ttf files from scripts/og-image-assets/ via readFileSync + base64, never next/font"

requirements-completed: [ASSET-01, ASSET-02]

coverage:
  - id: D1
    description: "public/og-image.png exists at exactly 1200x630px, compositing a cropped flat-matted frame of annotate-hero.png with headline/wordmark/tagline copy and purple accents on 'annotate' and the chevron"
    requirement: ASSET-01
    verification:
      - kind: other
        ref: "node -e IHDR width/height check on public/og-image.png (bun run generate:og && ...) — see plan Task 3 automated verify"
        status: pass
      - kind: manual_procedural
        ref: "Visual inspection of public/og-image.png via Read tool — confirmed annotation cluster legible, flat matted frame, purple accents correctly placed, text panel unclipped"
        status: pass
    human_judgment: false
  - id: D2
    description: "scripts/generate-og-image.ts regenerates the identical 1200x630 PNG deterministically via bun run generate:og, with zero network dependency (fonts + hero image base64-inlined)"
    requirement: ASSET-01
    verification:
      - kind: other
        ref: "bun run generate:og (ran twice during execution — Task 3 generation + re-verify), both produced 1200x630 output; bun run build exits 0 confirming the script type-checks within tsconfig scope"
        status: pass
    human_judgment: false
  - id: D3
    description: "ASSET-02 (LinkedIn Post Inspector live render) — structurally satisfied: file exists at exact dimensions and og:image tag (plan 03-01) references it absolutely via metadataBase resolution"
    requirement: ASSET-02
    verification: []
    human_judgment: true
    rationale: "Live LinkedIn Post Inspector render requires a deployed URL, which does not exist until Phase 4 per the plan's own backstop — cannot be automated or verified in this plan."

duration: 12min
completed: 2026-07-24
status: complete
---

# Phase 3 Plan 2: OG Social Card Generation Summary

**Reproducible 1200x630 public/og-image.png generated by a standalone Bun+Playwright script that screenshots a base64-self-hosted HTML template compositing a cropped annotate-hero.png frame with brand copy.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-07-24T04:32:00Z
- **Completed:** 2026-07-24T04:44:01Z
- **Tasks:** 3
- **Files modified:** 5 (2 fonts created, 1 script created, 1 package.json modified, 1 PNG created)

## Accomplishments
- Committed Shantell Sans 700 and Public Sans 600 as standalone `.ttf` files under `scripts/og-image-assets/`, downloaded once from the official `fonts.gstatic.com` CDN, giving the OG generation script zero runtime network dependency
- Wrote `scripts/generate-og-image.ts`: a standalone Bun script that base64-inlines both fonts + `public/annotate-hero.png`, builds a self-contained 1200x630 HTML template implementing the locked 03-UI-SPEC.md layout (left text panel, right flat white-matted cropped photo), and screenshots it with Playwright's bundled Chromium
- Generated and committed `public/og-image.png` at exactly 1200x630, visually confirmed legible and on-brand
- Wired `"generate:og": "bun scripts/generate-og-image.ts"` into `package.json`, matching the existing `verb:noun` script-naming convention (`verify:claims`)

## Task Commits

Each task was committed atomically:

1. **Task 1: Acquire and commit the two OG-card font files** - `ac79e8b` (feat)
2. **Task 2: Write scripts/generate-og-image.ts + add the generate:og package script** - `251f16c` (feat)
3. **Task 3: Generate public/og-image.png and verify exact 1200×630 dimensions + crop legibility** - `c604af5` (feat)

**Plan metadata:** (this commit, pending)

## Files Created/Modified
- `scripts/og-image-assets/ShantellSans-700.ttf` - Committed font binary (392,812 bytes), downloaded once from `fonts.gstatic.com`
- `scripts/og-image-assets/PublicSans-600.ttf` - Committed font binary (56,244 bytes), downloaded once from `fonts.gstatic.com`
- `scripts/generate-og-image.ts` - Standalone Bun script: base64-inlines fonts + hero image, builds the UI-SPEC layout as an HTML string, screenshots via `@playwright/test`'s bundled `chromium` at exactly 1200x630, writes `public/og-image.png`
- `public/og-image.png` - Committed 1200x630 OG social card output
- `package.json` - Added `"generate:og": "bun scripts/generate-og-image.ts"`

## Decisions Made
- Kept the UI-SPEC's default crop rectangle (`x:400,y:170,w:780,h:630`) unchanged — the generated card's annotation cluster (the "primitive types" boxed callout, "strong vs weak idk actly" arrow+underline, "Arrays with int?" note, "Never seen this prob..." bracket-underline) is clearly legible and not awkwardly cut, satisfying the plan's visual-QA backstop without needing adjustment
- Expressed the crop-to-box scaling as a runtime "cover" formula (`Math.max(widthRatio, heightRatio)`) driven by named `CROP_*`/`PHOTO_BOX_*` consts, rather than hardcoding the final `background-size`/`background-position` pixel values from 03-RESEARCH.md's worked example — functionally identical output, but keeps the four crop numbers a one-line, self-documenting edit if a future tuning pass is needed

## Deviations from Plan

None - plan executed exactly as written. The two backstop items in the plan frontmatter were both handled as specified: the crop rect was inspected and accepted as legible (no adjustment needed), and ASSET-02's live LinkedIn render was correctly left deferred to Phase 4 (structural satisfaction only — file exists at 1200x630, `og:image` tag from plan 03-01 references it absolutely).

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `public/og-image.png` exists and is committed; the `og:image` meta tag added in plan 03-01 now resolves to a real, correctly-sized asset
- ASSET-01 fully satisfied; ASSET-02 structurally satisfied (file + absolute tag reference) — the live LinkedIn Post Inspector check remains a Phase 4 deploy-time verification step, not a blocker for Phase 3 completion
- No blockers for Phase 3 wrap-up or Phase 4 planning

---
*Phase: 03-seo-metadata-structured-data-social-card*
*Completed: 2026-07-24*

## Self-Check: PASSED

All created files found on disk; all task commit hashes (`ac79e8b`, `251f16c`, `c604af5`, `e757a42`) found in git log.
