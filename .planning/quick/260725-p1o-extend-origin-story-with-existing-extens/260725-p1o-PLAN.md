---
phase: quick-260725-p1o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/content.ts
  - scripts/verify-claims.ts
  - app/page.tsx
  - app/globals.css
  - tests/landing.spec.ts
autonomous: true
requirements: [CONT-07]

must_haves:
  truths:
    - "The #origin-story card renders three paragraphs: the existing NUS CS2030 paragraph unchanged, followed by the two new author-voice paragraphs."
    - "The two new paragraphs read exactly as the author supplied them — informal register, the U+2026 ellipsis, and the missing terminal period after 'cheeks' all preserved."
    - "The three paragraphs are visually separated, not collapsed into one block, using the existing type scale."
    - "bun run build completes clean and bun run verify:claims passes with the new copy inside its banned-term scan."
    - "The Playwright gate (scripts/test-gate.sh) passes and now asserts the origin card has three paragraphs."
  artifacts:
    - lib/content.ts
    - app/page.tsx
    - app/globals.css
    - scripts/verify-claims.ts
    - tests/landing.spec.ts
  key_links:
    - "lib/content.ts originStory.addendum -> app/page.tsx #origin-story paragraphs (rendered by mapping, so page copy cannot drift from the content module)"
    - "lib/content.ts originStory.addendum -> scripts/verify-claims.ts textEntries (new copy is covered by the banned-term claim scan)"
    - ".origin p + p in app/globals.css -> sibling paragraph spacing (the existing .origin p rule sets margin:0)"
---

<objective>
Extend the "the story" card (`#origin-story`) on the landing page with two additional
author-voice paragraphs supplied verbatim by the author. The existing NUS CS2030
paragraph stays byte-identical; the new copy follows it.

Purpose: The origin story currently stops at the setup. The author supplied the punchline —
the failed search for an existing extension, and the ADHD-procrastination confession — which
is the part that actually makes the section land. This is personal narrative, not a product
claim, so it needs no STORE-LISTING.md traceability, but it must not smuggle in a new claim.

Output: Three-paragraph origin card, content-module-backed, spacing-corrected, claim-scanned,
and covered by a Playwright assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@lib/content.ts
@app/page.tsx
@app/globals.css
@scripts/verify-claims.ts
@tests/landing.spec.ts
@TESTING.md
</context>

<source_copy>
The author's verbatim copy. Two paragraphs. Reproduce character-for-character.

Paragraph A:
You would think someone has made a Chrome extension to do exactly this. I spent 1h searching for available extensions to annotate notes and they were all ass cheeks

Paragraph B:
So naturally… instead of studying CS2030, my ADHD brain decided to vibe coding this Chrome extension. To study CS2030 more efficiently. Yes.

Non-negotiable transcription rules:
- Paragraph A has NO terminating punctuation. It ends at the word `cheeks`. Do not add a period.
- Paragraph B uses a real single-character horizontal ellipsis (U+2026) after `naturally`. Do not
  substitute three ASCII periods.
- The informal register and the ungrammatical `decided to vibe coding` are the author's own voice
  for the author's own landing page. Do not correct grammar, soften slang, or paraphrase.
- Neither paragraph contains an apostrophe or a double quote, so no JSX entity escaping is needed.
  Do not introduce `&quot;`/`&apos;` where the author used none.
</source_copy>

<tasks>

<task type="auto">
  <name>Task 1: Add the two paragraphs to lib/content.ts with honest provenance, and bring them into the claim scan</name>
  <files>lib/content.ts, scripts/verify-claims.ts</files>
  <action>
    In `lib/content.ts`, extend the existing `originStory` object. Leave `text` and `source`
    byte-identical — `text` is the CONT-07 locked line whose provenance is `concept-1a.html`
    line 216 and which `01-VERIFICATION.md` records as a verbatim match; changing it would
    break that record.

    Add two new sibling keys inside the same `as const` object:
    - `addendum`: an array of exactly two string literals, Paragraph A then Paragraph B from
      `<source_copy>` above, transcribed under the non-negotiable rules stated there. Use single
      quotes to match the file's existing convention, and keep each paragraph as ONE unbroken
      single-line string literal (do not let a formatter split it across concatenated lines) so
      the verification greps below can anchor on the closing quote.
    - `addendumSource`: a provenance string recording the truth — supplied directly by the author
      (Kaleb Nim) for quick task 260725-p1o on 2026-07-25; first-person narrative continuation of
      the origin story; NOT drawn from design-handoff/concept-1a.html, STORE-LISTING.md, or
      README.md; informal register is intentional author voice and must not be rewritten; asserts
      no product, feature, or privacy claim. Do not imply design-handoff provenance it does not have.

    Also add a short comment above `addendum` noting that `app/page.tsx` renders these two entries
    by mapping over the array (unlike `text`, whose `<b>` wrap is reproduced directly in JSX), so a
    future editor knows the page is downstream of this array and not a second copy of it.

    In `scripts/verify-claims.ts`, extend the `textEntries` array so the new copy is inside the
    banned-term scan. Immediately after the existing `originStory.text` entry, spread in one entry
    per addendum paragraph, labelled `originStory.addendum[0]` / `originStory.addendum[1]` (derive
    the index — do not hand-write two duplicated literals). Change nothing else in that file: do
    NOT add to `BANNED_TERMS`, do NOT add to `WHITELISTED_PHRASES`, do NOT touch checks (2)-(5).
    The new copy is expected to produce zero matches on its own merits; if it produces one, that is
    a real signal to surface, not something to whitelist away.
  </action>
  <verify>
    <automated>grep -Fq "addendum" lib/content.ts && grep -Fq "ass cheeks'," lib/content.ts && grep -Fq 'So naturally… instead of studying CS2030' lib/content.ts && grep -Fq 'To study CS2030 more efficiently. Yes.' lib/content.ts && grep -Fq 'NUS CS2030 (Programming Methodology II)' lib/content.ts && grep -Fq 'originStory.addendum' scripts/verify-claims.ts && bun run verify:claims</automated>
  </verify>
  <done>
    - `originStory` exports `text` (unchanged), `source` (unchanged), `addendum` (2 entries), and `addendumSource`.
    - `grep -Fq "ass cheeks',"` succeeds, proving Paragraph A's string literal ends at `cheeks` with no added period.
    - The U+2026 ellipsis survives (the `So naturally…` grep is an exact byte match).
    - `bun run verify:claims` exits 0 and its output line reports a larger entry count than before (8 -> 10 copy entries).
  </done>
</task>

<task type="auto">
  <name>Task 2: Render the two paragraphs in the origin card and fix sibling-paragraph spacing</name>
  <files>app/page.tsx, app/globals.css</files>
  <action>
    In `app/page.tsx`, extend the existing `lib/content` import to also pull in `originStory`
    (the file currently imports only `faqs` from that module).

    Inside `<div className="origin" id="origin-story">` (around line 200), leave the existing
    `<p>` with its `<b>`-wrapped `NUS CS2030 (Programming Methodology II)` and its `&quot;`
    entities exactly as they are. After that `<p>`, render the addendum by mapping over
    `originStory.addendum`, emitting one `<p>` per entry with the string as its JSX text child and
    a stable `key`. Rendering from the array (rather than pasting two more literal JSX strings) is
    deliberate: it makes drift between the content module and the page structurally impossible.

    Render the strings as plain JSX text children — React escapes them automatically. Do NOT use
    `dangerouslySetInnerHTML` here; there is no markup in this copy that would justify it, and it
    would turn a static string into an injection surface for no benefit.

    In `app/globals.css`, the `/* origin */` block's `.origin p` rule sets `margin:0`, which was
    correct when the card held exactly one paragraph but collapses siblings into an unreadable
    block now that it holds three. Add a single new rule immediately after `.origin p`:
    `.origin p + p{margin-top:14px}`. Leave `.origin`, `.origin .tag`, and `.origin p` themselves
    untouched — the first paragraph must keep `margin:0` so the card's top padding is unchanged and
    the hand-drawn design fidelity is preserved. Add no other visual treatment: no new colors, no
    rotation, no border, no font change. 14px sits inside the existing spacing scale used elsewhere
    in this stylesheet and reads as roughly half the 17px/1.55 line box.
  </action>
  <verify>
    <automated>grep -Fq 'originStory' app/page.tsx && grep -Fq 'originStory.addendum' app/page.tsx && grep -Fq 'NUS CS2030 (Programming Methodology II)' app/page.tsx && ! grep -Fq 'dangerouslySetInnerHTML' app/page.tsx && grep -Fq '.origin p + p' app/globals.css && grep -Fq '.origin p{margin:0' app/globals.css && bun run build</automated>
  </verify>
  <done>
    - `#origin-story` contains three `<p>` elements: the original literal one, then two mapped from `originStory.addendum`.
    - The original paragraph's text, `<b>` wrap, and `&quot;` entities are unchanged.
    - `app/globals.css` has `.origin p + p{margin-top:14px}` and still has the original `.origin p{margin:0;...}` rule.
    - `bun run build` completes with no type or build errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Assert the three-paragraph origin card in the Playwright gate and run it</name>
  <files>tests/landing.spec.ts</files>
  <action>
    In `tests/landing.spec.ts`, inside the existing `test.describe('Section presence', ...)` block
    (alongside the FAQ-count and audience-chip tests around lines 137-143), add one test named for
    the behavior it checks — that the origin story renders all three paragraphs, ending on the
    CS2030 punchline. Assert that `#origin-story p` has count 3, and that the last of those
    paragraphs contains the text `To study CS2030 more efficiently. Yes.`

    Assert on rendered text via Playwright's locator text APIs; do not import from `lib/content` in
    the spec — `tests/` is excluded from tsconfig's type-check scope, and asserting on rendered DOM
    is what actually proves the page shipped the copy.

    Do not modify or weaken any existing assertion in this file, in particular the locked-URL
    assertions and the ten-section presence loop — extend only, per the repo's testing rules in
    `.claude/CLAUDE.md`.

    Then run the repo's real gate. `scripts/test-gate.sh` is the wired GSD test command; it runs
    the Playwright suite across all configured viewports.
  </action>
  <verify>
    <automated>grep -Fq 'origin-story p' tests/landing.spec.ts && bash scripts/test-gate.sh</automated>
  </verify>
  <done>
    - `tests/landing.spec.ts` has a new behavior-named test asserting three `#origin-story` paragraphs and the closing line.
    - No pre-existing assertion in the file was changed or removed.
    - `bash scripts/test-gate.sh` exits 0 with the full suite green (it must actually run, not self-skip).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content module -> rendered DOM | Static author-supplied strings flow from `lib/content.ts` into JSX. No request data, no user input, no network fetch crosses into this section. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p1o-01 | Tampering | `#origin-story` paragraph rendering in `app/page.tsx` | low | mitigate | Render addendum strings as JSX text children so React escapes them; `dangerouslySetInnerHTML` is explicitly forbidden for this copy and negatively asserted in Task 2's verify. |
| T-p1o-02 | Information disclosure | New landing-page copy | low | mitigate | New copy is personal narrative; Task 1 puts it inside `scripts/verify-claims.ts`'s banned-term scan so it cannot introduce an unverified product/privacy claim without failing the gate. |
| T-p1o-03 | Tampering | Package installs | low | accept | This plan installs no packages — `lib`, `app`, `scripts`, and `tests` edits only, zero `package.json` dependency changes. No supply-chain surface introduced. |
</threat_model>

<verification>
1. `bun run verify:claims` exits 0, reporting 10 copy entries scanned (was 8) with 0 unexpected banned-term matches.
2. `bun run build` completes clean — no TypeScript error from the widened `originStory` shape in either `app/page.tsx` or `scripts/verify-claims.ts` (both are inside tsconfig's `**/*.ts` include).
3. `bash scripts/test-gate.sh` exits 0 with the suite actually running (not hitting one of its self-skip branches).
4. Manual read-back of `lib/content.ts`: Paragraph A ends at `cheeks` with no period; Paragraph B carries the U+2026 ellipsis and the phrase `decided to vibe coding` uncorrected.
</verification>

<success_criteria>
- The origin card renders three paragraphs; the first is byte-identical to what shipped before.
- Both new paragraphs match the author's supplied text character-for-character, including the absent
  terminal period, the U+2026 ellipsis, the slang, and the ungrammatical phrasing.
- `lib/content.ts` records the new copy's real provenance (author-supplied, 2026-07-25) rather than
  implying it came from the design handoff.
- Paragraphs are visually separated by 14px with no other visual change to the card.
- `bun run verify:claims`, `bun run build`, and `bash scripts/test-gate.sh` all pass.
</success_criteria>

<output>
Create `.planning/quick/260725-p1o-extend-origin-story-with-existing-extens/260725-p1o-SUMMARY.md` when done
</output>
